import * as React from 'react';
import {Clickable} from '../../../../types/Types';
import {Icon} from './Icon';

export const SelectionIconButton = (props: Clickable) => (
  <Icon
    name="filter-variant"
    size="large"
    className="Icon-Button"
  />
);
